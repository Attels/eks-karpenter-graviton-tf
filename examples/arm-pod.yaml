apiVersion: v1
kind: Pod
metadata:
  name: arm64-hello
  labels:
    pod-name: arm64-hello
spec:
  containers:
    - name: hello
      image: nginx:stable
      ports:
        - containerPort: 80
      volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
      resources:
        requests:
          cpu: "100m"
          memory: "64Mi"
  volumes:
    - name: html
      configMap:
        name: nginx-html
  nodeSelector:
    kubernetes.io/arch: arm64
    karpenter.sh/nodepool: karpenter-nodepool-arm64
  restartPolicy: Never
